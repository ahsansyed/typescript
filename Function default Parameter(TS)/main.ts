function buildName(firstName : string, lastName = "khan") : string {
	if(firstName)
	return 
	firstName +""+ lastName;
	else
	return
	firstName
}
var result=buildName("umrao","Jaan");
var result=buildName("umri");